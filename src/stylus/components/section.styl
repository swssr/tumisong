.bridge
    background-color var(--primary)
    &::before
        content: '';
        position: absolute;
        width: 18vw;
        height: 100%;
        background-color: var(--secondary);
        top: 0;
        right: 0;
        bottom: 0;
        z-index -2;